import { Types as T } from "@lingdocs/ps-react";
import {
    nouns,
    verbs,
    adjectives,
    locativeAdverbs,
    adverbs,
} from "./words";

const entryFeeder: T.EntryFeeder = {
    nouns,
    verbs,
    adjectives,
    locativeAdverbs,
    adverbs,
}

export default entryFeeder;